@using WebApplicationTask.Controllers.Departments.Dtos

@model IEnumerable<DepartmentDto>

<h1>Departments</h1>

<ul>
    @foreach (var department in Model)
    {
        <li>
            <a href="@Url.Action("Details", new { id = department.Id })">@department.Name</a>
        </li>
    }
</ul>
